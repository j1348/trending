{
  "name": "trending-back",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-server": "babel src -d build src --presets es2015,stage-2",
    "clean": "rm -rf build && mkdir build",
    "build": "npm run clean && npm run build-server",
    "start": "node ./build/index.js",
    "dev": "PORT=4000 nodemon --exec babel-node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "prettier --single-quote --write --tab-width 4 --print-width 80 --trailing-comma all --write 'src/**/*.js'"
  },
  "keywords": [],
  "author": "me@jfroffice.me",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.4",
    "cron": "^1.3.0",
    "express": "^4.16.2",
    "graphql-type-json": "^0.2.1",
    "lodash": "^4.17.5",
    "lodash.omit": "^4.5.0",
    "memoizee": "^0.4.11",
    "mongoose": "^5.0.8",
    "trending-github": "^1.0.2",
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
